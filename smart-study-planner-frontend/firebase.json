{
  "firestore": [
    {
      "database": "smart-study",
      "rules": "firestore.rules",
      "indexes": "firestore.indexes.json"
    },
    {
      "database": "smart-study-prd",
      "rules": "firestore.rules",
      "indexes": "firestore.indexes.json"
    }
  ]
}
