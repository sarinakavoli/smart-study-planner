{
  "firestore": [
    {
      "database": "smart-study",
      "rules": "firestore.rules",
      "indexes": "firestore.indexes.json"
    },
    {
      "database": "smart-study-prd",
      "rules": "firestore.rules",
      "indexes": "firestore.indexes.json"
    }
  ],
  "storage": [
    {
      "bucket": "dev-sarina.firebasestorage.app",
      "rules": "storage.rules"
    },
    {
      "bucket": "prd-sarina",
      "rules": "storage.rules"
    }
  ]
}
